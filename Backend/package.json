{
  "name": "email-builder-backend",
  "version": "1.0.0",
  "description": "Backend server for email template builder",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "npm install --production && echo 'Creating public directory...' && mkdir -p public && echo '{}' > public/package.json && echo 'Build completed successfully! Use npm start to run the production server.'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudinary": "^2.5.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
